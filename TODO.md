# 待办与已知限制

记录尚未完成或需要人工验证的事项，供后续迭代参考。

## 需要人工验证（当前环境无法启动真实浏览器）

- [ ] GitHub Pages 上线后冒烟：`https://codesimplebird.github.io/masterPage/`
- [ ] 中文输入法（IME）实际输入时按 Enter 选词，确认不会误触 Bing 搜索
- [ ] 搜索历史键盘操作：`↑↓` 选择、`Enter` 搜索、`Esc` 关闭、`Tab` 移入历史下拉（现已改为 `focusout` 监听，需实测 Tab 移出时的关闭时机）
- [ ] 拖拽排序手感：手柄拖拽、FLIP 让位动画、落下回弹、松手后顺序持久化
- [ ] 浅色主题下检查滚动条、::selection、光晕层对比度
- [ ] 断网 / `file://` 打开时：index 内联数据可用，v3 回退到 fallbackConfig
- [ ] 移动端冒烟：抽屉菜单、焦点管理、触屏滚动（低优先级）
- [ ] 屏幕阅读器实测：搜索框 combobox 语义、结果数播报（`#search-status`）、历史 option 焦点管理

## 可选项（默认关闭，已注释在代码中）

- CRT 扫描线效果：`.scanlines` 样式已写入 CSS（注释状态），启用时取消注释并保持低透明度
- 结果计数在搜索框上方的提示行显示，若觉干扰可回退为仅高亮

## 后续可考虑（未开始）

- 移动端拖拽排序替代方案（编辑模式 / 上移下移按钮）
- 分类数据更多可视化（分类图标 hover 动效微调）
- 链接数量更多时考虑分页或折叠分类

## 最新一次优化要点（V3.0.12）

- 彻底根治伴侣“归位至屏幕右下角”按钮失效问题并升级平滑返航折跃动画（Fix Drone Reset & Warp Flight）：
  - 根因排查：
    1. 底层存储异常中断：`resetPosition()` 第一行执行 `LS.remove(POS_KEY)`，但在项目中 `LS` 封装对象仅声明了 `get` 和 `set` 方法，未定义 `remove` 方法！导致点击按钮瞬间立即抛出 `TypeError: LS.remove is not a function`，未捕获异常直接终止了后续所有的坐标重置代码；
    2. 样式突变与无动画：旧逻辑直接设置 `left: 'auto'; top: 'auto'`，在 CSS 规范中，从像素值向 `auto` 的过渡无法生成补间动画，且无法与屏幕视窗 resize 坐标模型优雅衔接；
    3. 工具按钮微小点击热区：气泡头部由于集成了多种特色玩法按钮，旧版宽度（240px）较为局促，工具按钮热区较小；
  - 修复与重构落地：
    1. 在 `LS` 存储适配器中补齐安全的 `remove(key)` 方法（封装 `localStorage.removeItem` 并附带 try-catch 静默降级）；
    2. 重写 `resetPosition()` 航标校准算法：动态计算当前屏幕分辨率下的右下角目标绝对坐标，触发 `left/top 0.45s cubic-bezier(0.25, 1, 0.5, 1)` 丝滑返航飞行动画，伴随机身 360° 腾跃空翻与专属返航播报；返航着陆后优雅切换回标准的 `right: 28px; bottom: 28px` 锚点；
    3. 气泡宽度优化至 275px，工具栏按钮点击热区扩大，兼顾多形态徽章与工具栏图标的高效排布；
    4. 通过真实的 Microsoft Edge 浏览器 CDP（Chrome DevTools Protocol）环境进行全真自动化回归测试，确认 0 报错、平滑飞行动画 100% 达成。

## 历史优化要点（V3.0.11）

- 赛博桌面伴侣战术红外激光逗宠与摸鱼互动系统全面重构（Laser Pointer Chase & Tease 2.0）：
  - 彻底根治“没到激光点就秒结束”的触发与判定缺陷：
    - 根因排查：此前启动游戏时，光标正处于伴侣气泡工具栏按钮上（与机甲机身物理间距过近），且代码设定为只要靠近 `dist <= 28px` 且累计 3 次即刻调用 `stopLaserGame()` 强制退出。导致用户点击开始后，手腕微动即在 300 毫秒内连续触发 3 次捕获并直接强行关闭，造成“光斑瞬间消失、机甲未追逐就结束”的糟糕体验；
    - 修复与重塑方案：
      1. 安全距离与起动延迟：初始红外激光瞄准点生成在机身对侧屏幕视窗内，并赋予前置 1.0 秒免触安全期以及需移开光标超过 60px 后的 `isArmed` 激活保护，100% 杜绝误触秒触发；
      2. 移除 3 次强制关闭限制：游戏不会再自作主张强行退出！玩家可以尽情在屏幕上逗引机甲，直至主动点击顶部退出按钮或敲击 `Esc` 键退出；
  - 赋予逗宠真正的玩法意义与解压陪伴价值（Playful Mechanics & Tease System）：
    - 屏幕顶部专属战术 HUD 悬浮控制条（`.drone-laser-hud`）：
      * 实时显示捕获次数、当前连击数（`COMBO`）、累计获得好感度；
      * 贴心操作导引提示，并配备一键退出按钮（`退出 (ESC)`），退出时弹出综合战绩结算气泡；
    - 戏耍与扑空判定（Teasing & Pouncing Reflex）：
      * 侦测光标瞬间移速（`pointerSpeed`）：当机甲扑咬冲刺的一瞬，若玩家手速极快甩开光标，机甲将扑空硬直并抓狂吐槽（如“嗷呜！扑空了！主人手速好快！”、“可恶，等离子爪抓了个寂寞！”），连击清零；
      * 若玩家适度停顿或减速，机甲将精准扑中光斑：爆发等离子光爆、空中 360° 腾跃空翻，连击数 +1，并飞升金色符文（`🎯 扑获+1 ⚡ COMBO xN`）；
    - 成长激励与里程碑：
      * 每次捕获奖励伴侣好感度 +3、电量补充 +4%；
      * 连击达到 3 连击以上触发机甲高能超神语音，达成 5 次与 10 次捕获触发胜利号角并额外奖励好感度 +5！
  - 作用域变量重构与语法加固：
    - 将拖拽模块内的光标捕获变量重命名为 `dragStartX`/`dragStartY`，消除与激光瞄准坐标的潜在同名遮盖风险；
    - 100% 通过 Node 运行时全真测试与结构化机械对齐校验。

## 历史优化要点（V3.0.10）

- 修复伴侣初始化 TDZ 暂时性死区异常（Fix ReferenceError: Cannot access 'widget' before initialization）：
  - 根因定位：在 `setupDroneCompanion` 函数内部，`const widget = els.droneWidget;` 原先声明在靠后的拖拽模块中，而前置的 `updateUI` -> `applyChassis` 在执行时试图读取 `widget`，触发了 JavaScript ES6 块级作用域的暂时性死区（TDZ），导致后续页面链接看板的 `render()` 及组件初始化被中断白屏；
  - 修复方案：将 `const widget = els.droneWidget;` 及相关全局标志位（`isRingOpen`, `isPointerDown`, `isDragging`）移至 `setupDroneCompanion` 函数最顶端前置初始化，彻底根除变量访问死区，页面初始化与看板渲染 100% 顺畅执行！

## 历史优化要点（V3.0.9 - Round 4 进化）

- 赛博桌面伴侣生产力与健康守护哨兵矩阵（Health Sentry, Memo Sync & Audio Suite）：
  - 久坐远眺与 30 秒赛博深呼吸引导 (Health & Eye Care Sentry)：
    - 气泡工具栏集成 `🌿` 绿色健康入口（亦支持连续工作 45 分钟自主关怀触发）；
    - 呼出高科技全息深呼吸浮层（`.drone-health-overlay`），引导主人进行科学调节：
      * 吸气阶段 (4s)：全息光环平滑膨胀，注入渐升空灵环境音；
      * 屏息阶段 (3s)：金光蓄能收敛；
      * 呼气阶段 (4s)：光环柔和收缩，注入渐降松弛舒缓音；
    - 伴随远眺 20 秒护眼与腰背伸展提醒，完成引导奖励好感度 +6、电量 +10%！
  - 侧边栏便签待办动态感知联动 (Memo Sync Sentry)：
    - 伴侣自主轮询侧边栏便签（`v14-memos`）：
      * 侦测到存在未勾选的待办事项时，机身天线信标点亮亮黄色警示脉冲呼吸（`.drone-beacon.has-memos`）；
      * 悬浮提示清晰显示未办任务数量，贴心协助主人闭环工作流；待办全数清空后恢复常态隐蔽。
  - 原生 Web Audio API 全维度音频套件升级：
    - 注入舒缓深呼吸音（`inhale` / `exhale`）、功德木鱼音（`woodfish`）、激光猎捕音（`catch`）、机甲变形流光音（`transform`），纯前端数字算法合成，零外部依赖！

## 历史优化要点（V3.0.8 - Round 3 进化）

- 赛博桌面伴侣物理趣味摸鱼小游戏矩阵（Physics Mini-Games: Laser Chase & Cyber Woodfish）：
  - 小游戏 1：战术红外激光逗引与物理猎捕引擎 (Tactical Laser Pointer Chase)：
    - 气泡工具栏集成 `🎮` 快捷入口（支持 Esc 随时退出）；
    - 屏幕呼出高能脉冲激光指示点（`.drone-laser-dot`，带双环旋转脉冲）；
    - 伴侣进入猎犬突击模式（`.is-hunting`），推进器喷口狂喷等离子火花，使用动态阻尼逼近算法在屏幕上飞速追猎光斑；
    - 逼近 < 28px 时触发“扑咬拦截”动作：爆发暴击等离子冲击波，触发 360° 空中腾跃（Barrel Roll）与欢呼电音，并奖励好感度 +3、电量 +5%；
    - 连续成功捕获 3 次达成大获全胜通关奖励！
  - 小游戏 2：赛博解压电子木鱼与功德祈福模式 (Cyber Wooden Fish / Karma Tapper)：
    - 气泡工具栏集成 `📿` 一键切换木鱼禅定态（`.is-zen-mode`，机身金光呼吸光晕）；
    - 每次轻戳机身或点击触发清脆治愈的赛博电子木鱼声波（Web Audio 算法还原木质谐振与混响）；
    - 机顶向外喷涌发散高光金色浮动字样（`功德 +1 ⚡`、`Bug -1 🛡`、`算力 +10 💻`、`头发 +1 💇`、`带薪摸鱼 +1s ☕`、`免测上线 +1 🚀`、`工资翻倍 +1 💰`、`心平气和 +1 🧘`）；
    - 累计敲击 10 次触发功德圆满和弦，奖励好感度 +5、电量 +10%！减压摸鱼利器。

## 历史优化要点（V3.0.7 - Round 2 进化）

- 赛博桌面伴侣好感度羁绊、自主人格与昼夜作息感知系统（Affinity, Moods & Circadian Autonomy）：
  - 深度好感度与四阶羁绊系统 (Affinity & Bond System, 0 ~ 100，持久化 `state.affinity`)：
    - 多维正向互动增进亲密度：摸摸头 (+2)、成功投喂 (+5)、达成 25 分钟番茄专注守望 (+15)；
    - 四阶专属羁绊称号：
      * `0 ~ 29`：`[STRANGER 陌生主机]`
      * `30 ~ 69`：`[PARTNER 战术搭档]`
      * `70 ~ 89`：`[INTIMATE 亲密伙伴]`
      * `90 ~ 100`：`[SOULMATE 灵魂契约]`
    - 对话气泡标题栏常驻高精好感度徽章（`♥ 50 PARTNER`），单击徽章可唤出好感度与羁绊详情气泡；
    - 好感度提升或狂喜时，机体向外喷涌发散高光浮动爱心粒子（`.drone-heart-particle`）！
  - 动态情绪状态机与无聊自娱自乐自主行为 (Boredom Autonomy Engine)：
    - `is-ecstatic (狂喜)`：好感度 >= 85 且电量充沛时，通体散发粉蓝心动霓虹光晕；
    - `is-bored (无聊挂机)`：当检测到鼠标与页面静止超过 60 秒时，机甲自主触发打哈欠伸展动效（`is-stretching`）、机体 360° 自传与自娱自乐碎碎念（“主人好一会儿没戳我了，本座自顾自打两套赛博太极拳...”）；
    - 主人光标活动恢复时，机甲灵敏察觉并触发欢快回归问候！
  - 昼夜作息与环境感知 (Circadian & Real-time Context Awareness)：
    - **深夜模式 (23:00 ~ 06:00)**：自动降低机体外壳刺眼高光（`.is-night-owl`），天线信标切换为柔和暖黄护眼小夜灯，主动发出深夜早睡温馨提醒；
    - **清晨问候与午间干饭提醒**：感知本地时钟，分时段注入关怀语录。

## 历史优化要点（V3.0.6 - Round 1 进化）

- 赛博桌面伴侣形态演化、个性命名与模块化装甲矩阵（Chassis Forms, Naming & Dynamic Armor）：
  - 三大核心机体形态重构（全套独特 SVG 矢量骨架与机械微动效，状态跨会话持久化 `state.chassis`）：
    1. **Form 1: 游隼侦察型 (Falcon Scout)**：经典尖锐流线掠翼、空气动力矢量双翼（带微幅扑翼动效）、双等离子尾焰与高频信标；
    2. **Form 2: 泰坦重装球 (Titan Orb / Haro)**：重型浑圆防卫装甲体、两侧开合式动力散热耳鳍（`titan-ear-l/r` 呼吸摆动动效）、顶部装甲折线与同心雷达目镜；
    3. **Form 3: 灵动机械猫 (Cyber Neko Pod)**：软萌机体轮廓、一对折叠三角脉冲猫耳天线（`neko-ear-twitch` 偶发抖耳动效）、双侧微型脉冲胡须线刻、底部柔性数据流摇摆猫尾巴（`neko-tail-sway`）；
    - 全机型自适应共享中央高精光学眼（跟随注视、眨眼、摸头笑眼、闭眼休眠、断电 X 眼全机型 100% 完美兼容）；
    - 对话气泡标题栏内置一键无缝形态轮换按钮 `FALCON / TITAN / NEKO ⇄`，切换伴随机体量子流光缩放动画与专属声波音效！
  - 自定义伴侣个性命名系统 (Pet Identity & Renaming)：
    - 默认代号 `// A.X.E.L.`，气泡标题栏集成独立重命名按钮（`✎`）；
    - 支持一键交互式修改伴侣昵称（持久化保存在 `state.name`），伴随全域对话代号同步重注册；
  - 动态等级模块化装甲外挂组件 (Modular Dynamic Armor)：
    - 随着投喂等级（LV.1 ~ LV.5）动态加装外骨骼装甲片：
      - LV.1：超轻量裸机架构；
      - LV.2：加装肩部导流副翼（`.armor-lv2`）；
      - LV.3：加装胸腹高能防卫重装甲（`.armor-lv3`）；
      - LV.4：加装侧舷等离子充能导轨（`.armor-lv4`）；
      - LV.5（满阶）：点亮悬浮微型高能粒子星环（`.armor-lv5`，伴随全息呼吸脉冲辉光）。

## 历史优化要点（V3.0.5）

- 赛博桌面伴侣左键点击交互与多维对话系统全面升级（Interactive Pet Click & Dialogue Engine）：
  - 直接左键点击 100% 极速出对话：
    - 根除指针捕获缺陷：移除 `pointerdown` 阶段对 `widget` 的过早 `setPointerCapture`，改为仅在真实位移超过 6px 时才锁定指针，彻底恢复机身各子部件（核心、双翼、机壳、电量 HUD）的原生 `click` 事件派发；
    - 释放阻尼防误触机制：拖拽释放时增加 60ms 缓冲，确保拖拽甩手机体后不会误触发点击；
    - 双重事件挂载与 120ms 防重：在 `widget` 与 `droneCore` 统一绑定分发，点击机甲任何部位秒级触发互动；
    - 单击对话与战术轮盘联动：无论是展开还是收起轮盘，单击直接触发机身灵巧微颤动画、音效并即刻弹出全息对话泡泡（绝电状态提示除颤、休眠状态点击唤醒）；
  - 全息对话气泡智能靠边防裁切（Viewport Edge Auto-Alignment）：
    - 针对机甲常驻屏幕右侧（`right: 28px`）导致居中气泡向右伸出屏幕外被裁切的问题，新增 `.bubble-align-right` 自适应规则；当机甲靠近视窗右边缘时，气泡与全息导引箭头自动向右对齐，确保文本与关闭按钮 100% 完整可视；
  - 对话语料库深度扩充（52+ 条赛博摸鱼/打工/陪伴语料）：
    - 新增即时戳戳与俏皮反应（原地转圈、傲娇抱怨散热口冒烟、赛博逗猫专家成就）；
    - 新增程序员与打工人幽默（祖传注释、带薪如厕算法、后台反向传播伪装、永远不 git push 永不故障）；
    - 新增温情守护与关怀（看绿植做眼球保养、浩瀚网络唯一锁定的超级主机、体温余温）。

## 历史优化要点（V3.0.4）

- 赛博桌面伴侣电子宠物养成与趣味博弈机制（Cyber-Pet Tamagotchi Gamification）：
  - 核心电量系统（Core Battery HUD System）：
    - 机身顶端常驻微型赛博电量 HUD（`.drone-batt-hud`），由高科分割式进度条与实时百分比读数组成；
    - 自然放电机制：机体伴随悬浮运转随时间平缓消耗电量（休眠时大幅降耗）；
    - 充能交互：投喂数据碎片恢复 +30% 电量，轻抚摸头静电充能恢复 +3%，25分钟专注模式圆满达成瞬间满血复活（100%）；
    - 电量不足发牢骚（Low Battery Whining）：电量低于 25% 时机体进入低电量警戒，推进器断续闪烁，瞳孔疲惫耷拉，并不时弹出委屈牢骚泡泡（如“推进器功率不足，要罢工了...”、“能量见底，快拿数据碎片来供奉我！”、“低电量警报，急需赛博咖啡因补给...”）；
    - 绝电休克与急救电击（Dead State & Defibrillator Revival）：电量耗尽至 0% 时机身断电下坠倾斜沉睡，光学眼变为死机 X 型（`drone-dead-eye`），推进器与全息光环熄灭，通体灰度离线（`filter: grayscale(0.9)`）；单击机身仅触发心跳平线告警音；**双击断电机身触发高压除颤脉冲（⚡ DEFIBRILLATOR）**，产生剧烈放电冲击波并成功重启系统（充能恢复至 25%）；
  - 投喂风险博弈与降级机制（Feeding Risk & Degradation Logic）：
    - 拒绝千篇一律的无脑升级，引入阶梯式风险矩阵：
      - LV.1 -> LV.2：80% 升级成功，20% 消化不良 Glitch；
      - LV.2 -> LV.3：65% 升级成功，20% 故障，15% 核心过载退回 LV.1；
      - LV.3 -> LV.4：50% 升级成功，25% 故障，25% 核心过载退回 LV.2；
      - LV.4 -> LV.5：35% 升级成功，35% 故障，30% 核心过载退回 LV.3；
      - LV.5（满阶）：60% 触发绝对暴击力场（电量直接充满 100%），40% 能量熔断过热降回 LV.4；
    - 降级伴随机身剧烈警报红光闪烁、过载震颤（`overload-shake`）与沉痛降级电音，趣味性与互动博弈感翻倍！

## 历史优化要点（V3.0.3）

- 交互娱乐与摸鱼消遣模块升级上线（方案 3 + 方案 4）：
  - 全屏自由拖拽独立全息桌面伴侣（Floating Cyber Drone Desktop Companion）：
    - 彻底脱离侧边栏，成为类似 Codex / AI 客户端的独立全屏悬浮桌面宠物，彻底释放侧边栏垂直高度与清爽视觉；
    - 侧边栏 `MOUSE_FX` 标题栏无缝整合 `⚡ HACK` 快捷启动按钮，随时一键启停黑客终端；
    - 全物理拖拽与视窗边界吸附系统：Pointer Events 像素级捕捉，视窗边界自动限制（防止拖出屏幕），释放时带平滑速度阻尼惯性滑行，位置自动跨会话持久化（`v14-drone-pos`），支持窗口 resize 自动校准；
    - 智能防遮挡全息对话气泡：接近屏幕顶端时自适应向下翻转（flip-down），内置打字机流式动效，支持 4.5s 自动淡出或点击 ✕ 快捷关闭；
    - 纯粹一体化机身交互：彻底移除机体外部冗余的附加齿轮按钮与浮动角标，**单击宠物机身本身即可直接唤出/收起战术选择轮盘**；
    - 彻底根除悬停抖动 BUG：修复了选项在鼠标悬停时因重置位移导致“悬停->位移离开光标->失去悬停->返回光标下”的高频震颤死循环，悬停放大时严格保持极坐标平移不变，在鼠标静止时 100% 稳如磐石；展开时自动暂停机体上下微幅漂浮，保证极高操作稳定性；
    - 灵动生命感与微表情互动（Life-like Pet Interactivity）：
      - 治愈摸头杀（Petting Detection）：光标在机身上来回轻抚 3 次触发欢快摸头杀，光学眼球转为向上月牙笑眼 `(˶ᵔ ᵕ ᵔ˶)`，机体微颤并发出机械呼噜声；
      - 自然随机眨眼（Random Blinking）：每 3.5~8 秒智能眨眼，让机体充满活泼的赛博生命力；
      - 推进器等离子拖拽尾迹（Plasma Exhaust Trail）：鼠标拖拽疾行时推进器喷口实时喷射等离子微光火花，高速甩出机身触发 360° 空中战术滚筒翻滚（Barrel Roll）；
    - 25 分钟赛博番茄专注守望模式（25M Pomodoro Sentry）：
      - 轮盘直接集成专注伴学/伴工模式，启动后机体上方浮现高科技倒计时 HUD 徽章 `[ 25:00 ]`，机体进入哨兵戒备守望态；
      - 达成 25 分钟时播放胜利号角音效、旋转空翻庆祝，并注入双倍数据碎片跃升机能；点击倒计时可随时暂停或中止；
    - 纯正 AAA 科幻矢量 HUD 质感（拒绝土味 Emoji）：
      - 轮盘全部升级为精美科幻矢量 SVG 战术线框图标（终端代码雨 `>_`、专注秒表 `⏱`、扫描雷达 `◎`、能量电池 `🔋`、纳米棱镜 `🎨`、休眠新月 `💤`）；
      - 全息激光连接线（Laser Spokes）：轮盘展开时虚线激光自机身中心向 6 大节点贯通投影；
      - 即时战术 HUD 悬浮指示卡（Instant HUD Tag）：光标掠过节点时中心即时浮现高科技指令代号（如 `[ ⚡ HACK_TERMINAL ]`、`[ ⏱ FOCUS_SENTRY ]`），零延迟反馈；
      - 对话气泡工具栏：集成快捷归位（`⌖`）、音效一键静音/开启（`🔊/🔇`）、关闭（`✕`）；
    - 战术环形轮盘菜单（Tactical Radial Ring Menu）：向四周正交展开 6 大战术指令节点：
      1. `⚡ HACK`：一键呼出全屏代码雨与黑客狂暴打字终端；
      2. `⏱ FOCUS`：启动 25 分钟赛博番茄专注伴侣；
      3. `🎲 RADAR`：摸鱼雷达全域扫描与今日赛博运势测算（摸鱼指数、宜忌、今日吉签）；
      4. `🔋 FEED`：直接投喂数据碎片充能升级（LV.1 ~ LV.MAX 满阶超频进化，双击机身亦可快速投喂）；
      5. `🎨 COATING`：一键轮转光学纳米涂装（霓虹青、矩阵绿、熔岩橙、洋紫晶，满阶进化解锁黄金圣甲）；
      6. `💤 SLEEP/WAKE`：低功耗休眠（闭眼 + 浮动 zZ 字符，瞳孔注视休眠，极低能耗）与一键警戒唤醒；
    - 趣味深度互动：单击机身展开选择的同时伴有机身灵动机巧震颤、蜂鸣音与 24+ 条幽默摸鱼语录；数据持久化于 localStorage（`v14-drone-state`、`v14-drone-sound`）；
    - 纯原生 Web Audio API 无外部依赖数字合成器：轻量生成机械哔哔声、抚摸呼噜声、开机音效、进化和弦、专注号角与休眠/唤醒提示音；
  - 黑客帝国代码雨与终端狂暴打字彩蛋（Matrix Rain & Hacker Typer）：
    - 按键盘反引号键（`` ` `` / `~`）或点击 `⚡ HACK` 按钮随时呼出全屏沉浸矩阵；
    - 全屏 Canvas 代码雨，鼠标移动触发激光扰动斥力与高光激发光波；
    - 居中悬浮 CRT 赛博控制台，键盘任意乱敲极速喷涌好莱坞级内核漏洞利用代码；
    - 破壁进度条实时攀升，达成 100% 触发高燃 ACCESS GRANTED 通关印章与 8-bit 合成电音（内置一键静音）；
    - 搜索框输入 `hack` 或 `matrix` 即可直接触发彩蛋；
    - 严格性能控制：仅在浮层打开时启动 requestAnimationFrame，退出（Esc / ✕）或切入后台时立即彻底停转，闲置 CPU/GPU 占用绝对为 0%。
- 严格遵守发布与推送规范：禁止自行执行 `git push`，待用户明确下达推送指令后再推送。

## 历史优化要点（V3.0.2）

- 外设通信绿色节能架构（彻底根治无线键盘异常快速耗电问题）：
  - 硬件配置与 EEPROM 真实性排查：全面审计 WebHID 通信代码，确认未修改键盘底层任何设置（仅发送纯只读 `0x20` 查询指令，未触碰 EEPROM/Flash，背光定时、按键消抖及固件出厂参数 100% 保持原样）。
  - 根因定位（射频高频打断与防休眠陷阱）：此前版本采用 15s 激进轮询，且循环向 2.4G 接收器全部 5 个 HID 接口广播发包；无线键盘固件通常在无输入/无无线通信 60~90s 后进入 <0.05mA 深度睡眠。旧机制每 15s 强行下发射频包唤醒 MCU，导致键盘 24 小时常驻 15~25mA 活跃接收态（若背光被激活则更高达 100~200mA），电池在 1~2 天内极速耗尽。
  - 绿色节能方案落地：
    1. 页面可见性挂载 (`document.visibilitychange`)：当标签页处于后台、最小化或 Windows 锁屏息屏时，立即彻底清除外设轮询定时器，通信频次降为绝对 0，允许键盘顺畅进入超低功耗深度休眠（<0.05mA，待机数月）。切回前台时静默查询 1 次并恢复低频轮询；
    2. 无线轮询周期大幅放缓：从 15s（键盘）/30s（鼠标）统一延长至 180s（3 分钟，降幅达 92%），既不打扰硬件休眠机制，又保留平稳电量采样；
    3. 通道精准定向过滤：`queryDevice` 仅向匹配 `targetUsagePage`（如 0xFF60 / 0xFFA1）的专属端点下发报文，彻底消除对系统标准键鼠接口的冗余广播；
    4. 保留 HUD 即时单击刷新与双击锁定，随时手动获取最新状态。
- 严格遵守发布与推送规范：禁止自行执行 `git push`，待用户明确下达推送指令后再推送。

## 历史优化要点（V3.0.1）

- 鼠标断开充电秒级即时响应与零延迟脱离：
  - 彻底根治断开延迟：鼠标硬件具备高精独立的充电标志位（被动 b10，主动 b9），断开充电线时底层报文立即置 0；解除旧版中将其强行卷入电量爬升 Sticky 锁存导致的"拔线后长时间滞留充电态"问题，断开瞬间立即切回放电水波动效，零秒延迟。
- 键盘充电检测灵敏度与有线接入智能识别：
  - USB 物理热插拔即时响应：接入键盘 USB 有线通信/供电接口时立即自动置位充电状态（⚡ 与激荡流动光效），拔出时即刻脱离；
  - 轮询刷新周期倍速提升：将键盘后台硬件遥测周期从 30s 提升至 15s，电量爬升侦测响应速度提升 100%；
  - 无线 2.4G 模式下支持双击任意 HUD 胶囊/侧边栏项目极速手动切换充电锁定，单击瞬时下发底层硬件遥测包；
  - 修复断开连接时 `.chip-liquid-fill` 未完全清空宽度的细节遗留。
- 严格遵守发布与推送规范：禁止自行执行 `git push`，所有改动仅在本地验证就绪，待用户明确允许后再行动。

## 历史优化要点（V2.14.16）

- 搜索框旁外设电池 HUD 动态高透流体波浪与显眼充电动效：
  - 电池流体容器：采用高透玻璃质感底色与从左至右渐变光底，微光映射与边框及当前电量挡位（红/蓝/绿）纯色同步；
  - 动态水波流动：双层径向光斑波浪以 GPU 加速 `transform: translateX` 驱动，形成从左向右不息奔流的液体波动效果（`mix-blend-mode: screen`）；
  - 显眼充电激荡态：外壳增加高光呼吸辉光，内部注入等离子充能光束（`batt-charge-surge`）与 3 倍速湍急水波（0.75s 循环），电光闪电符号高光跳动；
  - 鼠标电量校准与抗抖平滑引擎：
    - 硬件协议基准校准：从底层通信报文中提取经过固件校准后的主基准字段（解决之前因读取基础 ADC 端电压导致显示偏低 ~11% 的问题，与官方驱动 54% 基准完全对齐）；
    - 5点滑动窗口中位数滤波（Moving Median Filter）：消除鼠标快速移动时光学引擎瞬间功耗压降引起的 38%~43% 频繁跳动；
    - 放电单调防抖（Anti-Jitter Hysteresis）：在非充电时平滑过滤空载微小回弹杂讯（<=3%），真实连续下降时平稳跟随；

## 历史优化要点（V2.14.15）

- 鼠标与键盘全外设充电检测统一持久锁存与交互增强：
  - 根因修复：消除鼠标在 2.4G 接收器通信时被硬件位 0 强制冲掉充电锁定的问题，统一为键盘与鼠标双向 Sticky Latch 锁存机制；
  - 无论是键盘还是鼠标，在电量增长（爬升）或捕获硬件充电标志时自动锁定充电状态，平稳时不失锁，直至物理电量下降时才释放为放电；
  - 键盘与鼠标微型 HUD 与侧边栏行均统一支持双击手动切换充电锁存状态（单击刷新 / 双击切换），即插即看；
  - 严格遵守发布规范：所有远程推送（`git push`）必须由用户明确下达许可指令，禁止自行执行推送。

## 维护规则提醒

- 双文件同步：`index.html` 与 `index_v3.html` 的功能/样式/交互必须一致，仅数据加载不同
- 数据同步：`index.html` `config`、v3 `fallbackConfig`、`links.yaml` 三处保持一致
- 提交信息用 `git commit -F <utf8 文件>`：控制台直接传中文会触发 GBK 代码页损坏
- 提交但不推送，推送由用户明确确认
