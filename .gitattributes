# 统一行尾：仓库内与工作区一律 LF。
# 全局 core.autocrlf=true 会在检出时把 LF 改成 CRLF，导致每次提交刷警告，
# 并让 index.html / index_v3.html 的文本比对与数据 parity 校验失真。
* text=auto eol=lf

# 二进制资源不做行尾与文本转换
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.woff binary
*.woff2 binary
